# SDK log level (off | info | debug | trace)
sdk_log_level: "info"

# Properties to for connecting to Kinetic Task
task:
  server: "http://localhost:8080/kinetic-task"
  credentials:
    username: "admin"
    password: "admin"

#### HANDLERS TO UPDATE ####
handlers_to_update:

  ###  Add or remove handlers below as needed  ###

  # Kinetic Core kinetic_core_notification_template_send_v1 handler
  kinetic_core_notification_template_send:
    api_server:
    api_username:
    api_password:
    space_slug:
    smtp_server: "smtp.gmail.com"
    smtp_port: "25"
    smtp_tls: "true"
    smtp_username: ""
    smtp_password: ""
    smtp_from_address: ""

  # Kinetic Core Info (Set on Kinetic Core Handlers)
  kinetic_core:
    api_server: "http://localhost:8080/kinetic"
    api_username: "integration_user"
    api_password: "password"
    space_slug: "playground"

  # SMTP Info (Set on SMTP Handler)
  smtp_email:
    server: "smtp.gmail.com"
    port: "25"
    tls: "true"
    username: ""
    password: ""
